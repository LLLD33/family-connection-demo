{
  "name": "family-hot-news-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "A family hot news assistant that aggregates trends and generates parent-friendly scripts with Telegram delivery hooks.",
  "scripts": {
    "start": "node server.js",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
