{
  "name": "family-connection-demo",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight family connection assistant demo with web UI and Telegram bot notification hooks.",
  "scripts": {
    "start": "node server.js",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
